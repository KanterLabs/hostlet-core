# Staging gate canary

This non-product file exists only to exercise the exact-head staging approval,
revocation, and required-check workflow. The canary pull request is closed and
its branch deleted after the ruleset is verified.

This follow-up line proves that any synchronized head requires fresh approval.
